# simple-messaging-system-python

simple-messaging-system-python/
│
├── broker.py           # Broker class – holds topics, publishes and retrieves messages
├── producer.py         # Producer class – sends messages to the broker
├── consumer.py         # Consumer class – reads messages from the broker
├── main.py             # Simulation script – ties it all together
├── README.md           # Explains what this is and how to run it
└── .gitignore          # (Optional) Ignore Python artifacts like __pycache__/
